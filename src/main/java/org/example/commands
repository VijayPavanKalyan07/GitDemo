git init

-- project level
git config user.name pavankalyan
git config user.email vijaypavankalyanreddy@gmail.com

-- globally for entire system
git config -g user.name pavankalyan
git config -g user.email vijaypavankalyanreddy@gmail.com

git status

git add filename
git add .

git commit -m "Message"

git checkout branchname
-- git branch
git branch

// creating branch and checking out to that branch
git checkout -b <branchname>
git branch <branchname>

git merge <branchname>

-- to view commit history
git log

we can connect our local git repo to mutliple repos
In general its only 1 remote repo per local repo

"name of remote repo[key]" -> link

key -> origin

git remote add origin https://github.com/PavanPk170701/GitDemo.git




